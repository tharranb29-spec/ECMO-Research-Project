{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "a2a-dashboard.applicability-uncertainty.v1",
  "type": "object",
  "required": ["schema_id", "contract_version", "records"],
  "properties": {
    "schema_id": {"const": "a2a-dashboard.applicability-uncertainty.v1"},
    "contract_version": {"const": "1.0.0"},
    "records": {"type": "array", "items": {"type": "object", "required": ["assessment_id", "model_id", "similarity_threshold", "descriptor_distance_threshold", "inside_n", "outside_n", "r2_interval", "external_thresholds_frozen", "source"]}}
  },
  "additionalProperties": false
}
