{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "a2a-dashboard.evidence-inbox.v1",
  "type": "object",
  "required": ["schema_id", "contract_version", "records"],
  "properties": {
    "schema_id": {"const": "a2a-dashboard.evidence-inbox.v1"},
    "contract_version": {"const": "1.0.0"},
    "records": {"type": "array", "items": {"type": "object", "required": ["record_id", "count", "status", "disposition", "outcome_fields_loaded", "source"]}}
  },
  "additionalProperties": false
}
